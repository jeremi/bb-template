openapi: 3.1.0
info:
  title: Subscriptions API
  version: 1.0.0
  description: Subscription surface is missing a delete interface.
  contact:
    name: Sample BB Team
    url: https://example.org/contact
paths:
  /v1/subscriptions:
    post:
      operationId: createSubscription
      summary: Create a subscription
      description: Registers a webhook subscription.
      tags: [subscriptions]
      responses:
        '201':
          description: Created
    get:
      operationId: listSubscriptions
      summary: List subscriptions
      description: Lists webhook subscriptions.
      tags: [subscriptions]
      responses:
        '200':
          description: OK
  /v1/subscriptions/{subscriptionId}:
    get:
      operationId: getSubscription
      summary: Get a subscription
      description: Returns one webhook subscription but does not provide deletion.
      tags: [subscriptions]
      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: No Content
