asyncapi: 3.0.0
info:
  title: Section 10.11 pass
  version: 1.0.0
  description: contentType is plain JSON, and an explicit utf-8 charset is allowed.
  contact:
    name: GovStack Identity BB
    url: https://example.org/contact
defaultContentType: application/json
channels:
  userSignedUp:
    address: global.govstack.identity.v1.user.signedup
    messages:
      userSignedUp:
        $ref: '#/components/messages/UserSignedUp'
components:
  messages:
    UserSignedUp:
      name: UserSignedUp
      contentType: application/json; charset=utf-8
      payload:
        type: object
