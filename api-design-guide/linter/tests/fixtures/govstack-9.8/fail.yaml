openapi: 3.1.0
info:
  title: Section 9.8 fail
  version: 1.0.0
paths:
  /things:
    get:
      operationId: getThing
      responses:
        '200':
          description: A thing whose body schema is closed at the top level.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  id:
                    type: string
