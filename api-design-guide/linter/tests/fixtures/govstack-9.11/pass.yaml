openapi: 3.1.0
info:
  title: Section 9.11 pass
  version: 1.0.0
paths: {}
components:
  securitySchemes:
    oauth:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: https://example.org/token
          scopes:
            "bb:identity:person:read": Read persons
            "bb:identity:invoice:read": Read invoices
