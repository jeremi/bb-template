# 15.5 pass: cancellation is POST /v1/operations/{operationId}/cancel.
openapi: 3.1.0
info:
  title: Registry API
  version: 1.0.0
paths:
  /v1/operations/{operationId}/cancel:
    post:
      parameters:
        - name: operationId
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: accepted
