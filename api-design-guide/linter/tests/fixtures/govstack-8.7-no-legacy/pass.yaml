openapi: 3.1.0
info: { title: Structured RateLimit pass, version: 1.0.0 }
paths:
  /items:
    get:
      responses:
        "200":
          description: OK
          headers:
            RateLimit: { schema: { type: string } }
