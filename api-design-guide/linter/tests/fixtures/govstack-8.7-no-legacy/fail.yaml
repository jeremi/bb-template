openapi: 3.1.0
info: { title: Legacy RateLimit fail, version: 1.0.0 }
paths:
  /items:
    get:
      responses:
        "200":
          description: OK
          headers:
            RateLimit-Limit: { schema: { type: integer } }
            RateLimit-Remaining: { schema: { type: integer } }
            RateLimit-Reset: { schema: { type: integer } }
