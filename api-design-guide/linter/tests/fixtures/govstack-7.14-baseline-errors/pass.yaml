openapi: 3.1.0
info: { title: Baseline errors pass, version: 1.0.0 }
security:
  - oauth: []
paths:
  /v1/things/{thingId}:
    post:
      operationId: updateThing
      requestBody:
        content:
          application/json:
            schema: { type: object }
      responses:
        "200": { description: Updated. }
        "400": { description: Bad request. }
        "401": { description: Unauthenticated. }
        "404": { description: Not found. }
