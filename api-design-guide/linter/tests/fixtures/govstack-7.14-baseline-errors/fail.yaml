openapi: 3.1.0
info: { title: Baseline errors fail, version: 1.0.0 }
security:
  - oauth: []
paths:
  /v1/things/{thingId}:
    post:
      operationId: updateThing
      requestBody:
        content:
          application/json:
            schema: { type: object }
      responses:
        "200": { description: Updated. }
