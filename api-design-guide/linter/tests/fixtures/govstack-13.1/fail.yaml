# 13.1 fail: a scheme is declared but GET /v1/things is covered by neither a
# root-level nor an operation-level security requirement.
openapi: 3.1.0
info:
  title: Registry API
  version: 1.0.0
components:
  securitySchemes:
    OAuth:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: https://example.org/token
          scopes: {}
paths:
  /v1/things:
    get:
      responses:
        '200':
          description: ok
