# 14.1 fail: a create-POST (declares 201) does not accept an Idempotency-Key header.
openapi: 3.1.0
info:
  title: Registry API
  version: 1.0.0
paths:
  /v1/things:
    post:
      responses:
        '201':
          description: created
          headers:
            Location:
              schema:
                type: string
