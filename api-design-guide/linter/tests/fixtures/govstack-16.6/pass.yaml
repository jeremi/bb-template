openapi: 3.1.0
info:
  title: Payments Events API
  version: 1.0.0
  description: Webhook delivery declares the GovStack-Signature header.
  contact:
    name: Sample BB Team
    url: https://example.org/contact
webhooks:
  paymentCompleted:
    post:
      summary: Payment completed event
      parameters:
        - name: GovStack-Signature
          in: header
          required: true
          description: Detached JWS signature over the canonicalised event.
          schema:
            type: string
      requestBody:
        content:
          application/cloudevents+json:
            schema:
              type: object
      responses:
        '200':
          description: Acknowledged
