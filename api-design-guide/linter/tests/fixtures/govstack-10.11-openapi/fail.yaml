openapi: 3.1.0
info:
  title: Section 10.11 fail
  version: 1.0.0
paths:
  /v1/records:
    get:
      operationId: listRecords
      responses:
        '200':
          description: A page of records.
          content:
            # charset names an encoding other than UTF-8.
            application/json; charset=iso-8859-1:
              schema:
                type: object
