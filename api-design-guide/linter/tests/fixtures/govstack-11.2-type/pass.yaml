openapi: 3.1.0
info:
  title: Fixture API
  version: 1.0.0
  description: Fixture for govstack-11.2-type.
  contact: {}
paths:
  /v1/foos/{id}:
    get:
      operationId: getFoo
      summary: Get a foo
      description: Get a foo by id.
      tags: [foos]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '404':
          description: Not found
          content:
            application/problem+json:
              schema:
                type: object
                required: [type, title, status, traceId]
                properties:
                  type: { type: string, format: uri }
                  title: { type: string }
                  status: { type: integer }
                  traceId: { type: string }
              example:
                type: https://govstack.global/problems/identity/person-not-found
                title: Person not found
                status: 404
                traceId: 4bf92f3577b34da6a3ce929d0e0e4736
