# 13.4 fail: an OAuth scope string that follows none of the documented shapes.
openapi: 3.1.0
info:
  title: Registry API
  version: 1.0.0
components:
  securitySchemes:
    OAuth:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: https://example.org/token
          scopes:
            read_things: read things
paths:
  /v1/things:
    get:
      security:
        - OAuth: [read_things]
      responses:
        '200':
          description: ok
