openapi: 3.1.0
info: { title: Success schema pass, version: 1.0.0 }
paths:
  /v1/things:
    get:
      responses:
        "200":
          description: Thing response.
          content:
            application/json:
              schema:
                type: object
                description: A thing.
