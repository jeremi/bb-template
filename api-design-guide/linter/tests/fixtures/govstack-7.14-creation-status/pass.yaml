openapi: 3.1.0
info: { title: Creation status pass, version: 1.0.0 }
paths:
  /v1/things:
    post:
      operationId: createThing
      responses:
        "201": { description: Created. }
        "500": { description: Error. }
