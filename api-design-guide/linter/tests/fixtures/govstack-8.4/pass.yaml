openapi: 3.1.0
info:
  title: Sample API
  version: 1.0.0
  description: Accepts and echoes X-Request-Id.
  contact:
    name: Sample BB Team
    url: https://example.org/contact
paths:
  /items:
    get:
      operationId: listItems
      summary: List items
      description: List items.
      tags: [items]
      parameters:
        - name: X-Request-Id
          in: header
          required: false
          schema:
            type: string
      responses:
        "200":
          description: OK
          headers:
            X-Request-Id:
              schema:
                type: string
