openapi: 3.1.0
info:
  title: Sample API
  version: 1.0.0
  description: Fixture for govstack-10.10 (ISO 4217 currency codes).
  contact:
    name: Sample BB Team
paths:
  /v1/widgets/{widgetId}:
    get:
      operationId: getWidget
      summary: Get widget
      description: Get widget
      tags: [widgets]
      parameters:
        - name: widgetId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Widget'
components:
  schemas:
    Widget:
      type: object
      properties:
        currency:
          type: string
          pattern: '^[A-Z]{3}$'
