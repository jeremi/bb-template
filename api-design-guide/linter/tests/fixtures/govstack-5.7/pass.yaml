openapi: 3.1.0
info:
  title: Sample API
  version: 1.0.0
  description: A sample API with a plain CRUD path.
  contact:
    name: Sample BB Team
    url: https://example.org/contact
paths:
  /v1/events:
    post:
      operationId: createEvent
      summary: Create event
      description: Creates a new event.
      tags: [Events]
      responses:
        "201":
          description: The created event.
