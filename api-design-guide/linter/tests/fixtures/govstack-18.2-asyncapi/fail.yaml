# Fixture for govstack-18.2-asyncapi — logical channel ID major (v1) does not
# match info.version's major (2).
asyncapi: 3.0.0
info:
  title: Identity Events
  version: 2.0.0
  description: Emits identity lifecycle events for the Identity building block.
  contact:
    name: GovStack Identity BB
    url: https://example.org/contact
servers:
  production:
    host: '{brokerHost}'
    protocol: kafka
    variables:
      brokerHost:
        default: broker.example.org
channels:
  org.govstack.identity.v1.user.signedup:
    address: users/signed-up
    messages:
      userSignedUp:
        $ref: '#/components/messages/UserSignedUp'
operations:
  receiveUserSignedUp:
    action: receive
    summary: Receive user signed-up events.
    description: Triggered when a user completes registration.
    tags:
      - name: user
    channel:
      $ref: '#/channels/org.govstack.identity.v1.user.signedup'
    messages:
      - $ref: '#/channels/org.govstack.identity.v1.user.signedup/messages/userSignedUp'
components:
  messages:
    UserSignedUp:
      name: UserSignedUp
      title: User Signed Up
      payload:
        type: object
        properties:
          userId:
            type: string
            description: Identifier of the user.
